# dToken Provider Chrome App

Chrome App Mode version of the dToken Provider Console webpage.

Start:

```bash
npm run start
```

Or double-click `dToken Provider Chrome.app`.

Local URL: `http://127.0.0.1:8792/`

Stop:

```bash
npm run stop
```
