# dToken User Chrome App

Chrome App Mode version of the dToken User webpage.

Start:

```bash
npm run start
```

Or double-click `dToken User Chrome.app`.

Local URL: `http://127.0.0.1:8789/`

Stop:

```bash
npm run stop
```
